/*Dado el array [100, 200, 300], crea un nuevo array con los números divididos por 10 y muestra cada elemento en consola.*/

const numeros = [100, 200, 300];

let resultado = numeros.map(num => num / 10)
console.log(resultado);
